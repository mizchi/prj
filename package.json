{
  "name": "prj",
  "version": "0.0.0",
  "bin": "cli",
  "description": "mizchi's minimum project",
  "scripts": {
    "watch": "$(npm bin)/watchify -v -t coffeeify --extension='.coffee' -o bundle.js index.coffee",
    "build": "$(npm bin)/browserify -t coffeeify --extension='.coffee' -o bundle.js index.coffee",
    "open": "open -a /Applications/Google\\ Chrome\\ Canary.app 'http://localhost:9966'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^9.0.8",
    "coffeeify": "^1.0.0",
    "watchify": "^3.1.1"
  }
}
