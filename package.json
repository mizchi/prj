{
  "name": "prj",
  "version": "0.0.0",
  "bin": "cli",
  "description": "mizchi's minimum project",
  "scripts": {
    "watch": "$(npm bin)/watchify -v -t babelify --extension='.es6' -o bundle.js index.es6",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^9.0.8",
    "watchify": "^3.1.1",
    "babelify": "^5.0.4"
  }
}
